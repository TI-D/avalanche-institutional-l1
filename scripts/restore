#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
echo "Restoring validator identity from backup (NodeID must not change)"
if need_jq; then
  act restore-backup | jq '.state.recovery'
else
  act restore-backup
fi
