#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
need_jq
echo "Stage 1 model: writing a restore timestamp. Fence a real host before reusing a NodeID."
act restore-backup | jq '.state.recovery'
