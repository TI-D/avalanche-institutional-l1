#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
need_jq
echo "Stage 1 model: removing Validator 2 from JSON. No P-Chain transaction."
act remove-validator v2 | jq '{event: .state.events[0], validators: [.state.validators[] | {name,status}]}'
