#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
need_cli
avalanche network status
echo
if [[ -n "${NORTHSTAR_RPC:-}" ]]; then
  cast block-number --rpc-url "$NORTHSTAR_RPC" || true
fi
if [[ -n "${SETTLEMENT_RPC:-}" ]]; then
  cast block-number --rpc-url "$SETTLEMENT_RPC" || true
fi
