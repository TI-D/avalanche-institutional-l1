#!/usr/bin/env bash
set -euo pipefail
# shellcheck source=lib.sh
source "$(dirname "$0")/lib.sh"
need_jq
echo "Stage 1 model health (not AvalancheGo)"
api | jq '{mode: .state.mode, summary, validators: [.state.validators[] | {name,status,nodeId,az}]}'
