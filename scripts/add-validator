#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/lib.sh"
need_jq
echo "Stage 1 model: appending Validator 4 to JSON. No P-Chain transaction."
act add-validator | jq '{event: .state.events[0], validators: [.state.validators[] | {name,status}]}'
